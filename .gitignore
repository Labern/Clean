# Local Claude Code state (worktrees, local settings)
.claude/

# Nested git repositories with their own remotes — not tracked here
# ClaudeUsageMonitor → github.com/Labern/usage
ClaudeUsageMonitor/
Legacy/

# OS cruft
.DS_Store

# pica test fixtures (downloaded copyrighted PDF + derived extraction) + test deps
pica/tests/fixtures/
pica/tests/node_modules/
pica/tests/package.json
pica/tests/package-lock.json
# political → github.com/Labern/political
political/
# ignition → github.com/Labern/ignition (private)
ignition/
